const SEARCH_MODE = 'search';
const HIGHLIGHTS_MODE = 'highlights';
const ADMIN_MODE = 'admin';

const HIGHLIGHTS_OFFSET = 3;

export {
  SEARCH_MODE,
  HIGHLIGHTS_MODE,
  ADMIN_MODE,
  HIGHLIGHTS_OFFSET,
}